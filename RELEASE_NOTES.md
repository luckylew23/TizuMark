## ⬇️ 下载

> **🏆 推荐大多数用户选择：** [⬇ TizuMark_1.2.0_x64-setup.exe](https://gitee.com/tizu/TizuMark-Markdown-Editor/releases/download/v1.2.0/TizuMark_1.2.0_x64-setup.exe)
>
> **🛠 企业/批量部署：** [⬇ TizuMark_1.2.0_x64_en-US.msi](https://gitee.com/tizu/TizuMark-Markdown-Editor/releases/download/v1.2.0/TizuMark_1.2.0_x64_en-US.msi)
>
> **📦 绿色版（免安装）：** [⬇ TizuMark_1.2.0_x64.exe](https://gitee.com/tizu/TizuMark-Markdown-Editor/releases/download/v1.2.0/TizuMark_1.2.0_x64.exe)

### 三种安装包说明

| 安装包 | 适用人群 | 特点 |
|--------|---------|------|
| ⭐ **NSIS 安装包 (.exe)** — **推荐** | 绝大多数 Windows 用户 | 传统的 setup 向导安装，支持自定义安装路径、创建桌面快捷方式、自动注册文件关联。双击即装，即装即用。 |
| **MSI 安装包 (.msi)** | 企业 IT 管理员、需要批量部署的用户 | 标准的 Windows Installer 格式，支持组策略推送、静默安装（msiexec /i TizuMark_1.2.0_x64_en-US.msi /qn）、适合企业环境集中管理。 |
| **绿色版 (.exe)** | 追求便携的用户 | 单文件免安装，解压即用，适合 U 盘携带、临时使用，不写注册表。 |

---

## ✨ v1.2.0 更新内容

### 新增
- 代码块按需滚动条设置项（代码块超长时显示滚动条，避免撑开页面）
- 侧边栏文件目录排序（按名称／大小／时间）+ 显示文件大小与时间 + 右键菜单（打开所在目录、复制路径）
- 「关闭到托盘」快捷键（可自定义）；快捷键单键修改改为点「确认」才生效，避免误改
- 设置面板应用式改造 + i18n 全面补漏（中英文文案补全）
- 错误条移到底部（不再遮挡顶部工具栏）
- 后端健康探测横幅、文件夹监听异常兜底（更稳定）
- 大文档提示条新增「不再提醒」按钮（本次会话内不再弹，重启自动复位）

### 改进
- 大纲点击预览跳转改为顶部对齐（与编辑区一致，符合常见导航习惯）
- 设置弹框开关统一靠右对齐
- 导出 HTML 背景与提示框同灰、代码块 max-height 调整
- demo 图片 `\\?\` 路径前缀处理、mermaid 点击／分批渲染、主题切换 loading 平滑
- 列表缩进按 4 空格升一级、根治勾选框回写错位、无序列表黑点、截图 404 修复
- 插入图片对话框：文本框去掉不该有的下拉三角、关闭按钮改为文字「关闭」
- 图片加载失败时显示 alt 文本（含 [加载失败] 标记），不再只显示空白红框

### 修复
- 中文图片路径无法显示（解码渲染器对非 ASCII 路径的 percent-encode）
- Markdown 格式快捷键完全失效
- Ctrl+F 查找框输入一个字符即失焦
- 数学公式渲染：引用块内块级公式、表格内公式含 `|` 被切断、多相邻含 `|` 行内公式被跳过、跨段吞内容
- 架构升级后的滚动同步错位与勾选框跳动回归
- 点击大纲跳转后被恢复定时器覆盖不滚动
- 安全加固：脚注 XSS、fs 插件权限收窄、确认框 message XSS
- CI 稳定性：浏览器测试缺 puppeteer-core 时自动跳过；命令计数／权限格式门禁同步
- 依赖漏洞治理：to-ico→png-to-ico（npm audit 漏洞归零）

> 使用中遇到问题欢迎加 QQ 群：1035294939
