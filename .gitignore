# Dependencies
node_modules/

# Build output
dist/
target/
out/

# Generated renderer bundle — built from src/unified-renderer.js via scripts/build-renderer.mjs
# Regenerate with `npm run build:renderer` (also auto-run on `npm install` via the `prepare` script). Do NOT edit by hand.
src/lib/unified-bundle.js

# P2-3 vendor 锁定：以下 7 个库由 scripts/ensure-vendor.mjs 从 node_modules 确定性再生
# （接入 package.json "prepare"，CI 用完整 npm ci 自动重建）。请勿手改、勿提交。
src/lib/codemirror/
src/lib/katex/
src/lib/mermaid/
src/lib/html2canvas.min.js
src/lib/markdown-it.min.js
src/lib/html-docx.min.js
src/lib/highlight.js/

# Tauri
src-tauri/target/
src-tauri/WixTools/
src-tauri/Frameworks/
src-tauri/*.msi
src-tauri/*.exe
src-tauri/*.app

# Lock files (keep package-lock.json for reproducibility)
# pnpm-lock.yaml

# Environment
.env
.env.*

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Test files
test.md
.workbuddy/
.agents/
skills-lock.json

# Local tool data / build artifacts (not part of source)
release/
.git-rewrite/
.opencode/
ccstart.bat

# Ad-hoc debug scripts (临时调试产物，勿入库)
dbg-*.cjs
